{
  "name": "klask",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender-shell.mjs",
    "build:pages": "tsc -b && vite build --base=/klask/ && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender-shell.mjs && node scripts/copy-pages-404.mjs",
    "preview": "vite preview",
    "check:sim": "tsx scripts/check-simulation.ts",
    "check:types": "tsc -b",
    "check:viewports": "node scripts/capture-viewports.mjs"
  },
  "dependencies": {
    "ammojs-typed": "^1.0.6",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router": "^7.8.2",
    "three": "^0.176.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@types/three": "^0.184.1",
    "@vitejs/plugin-react": "^4.4.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=20"
  }
}
